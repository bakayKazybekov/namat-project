export type sliderSettings = {
  dots: boolean,
  arrows: boolean,
  infinite: boolean,
  slidesToShow: number,
  slidesToScroll: number,
  swipeToSlide: boolean,
  centerPadding: string,
  centerMode: boolean,
}